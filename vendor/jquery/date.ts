/*
let age: number = 20;

function getAge() : string {
    return age.toString()
}

function incrementAge() {
  age++;
}

function checkDate() {
  const today: Date = new Date();
  const Birthday: boolean = today.getMonth() === 9 && today.getDate() === 30;

  if (Birthday) {
    incrementAge();
  }
}

setInterval(checkDate, 24 * 60 * 60 * 1000); // Check every day

console.log(age);
*/